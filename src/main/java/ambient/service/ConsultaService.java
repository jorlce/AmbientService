package ambient.service;

public class ConsultaService {
	
	/*public ResponseEntity<Measure> ConsultaSensor(String sensorId) {
		 SensorData sensor = personMapRepository.findPerson(id);
		 return ResponseEntity.ok(person);
	}*/

}
